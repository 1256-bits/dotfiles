set -as terminal-features ",*:RGB"
set -s escape-time 2

unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

bind r source-file ~/.config/tmux/tmux.conf

set -g base-index 1
setw -g pane-base-index 1
set -g status-style 'bg=#4c566a fg=#5eacd3'
